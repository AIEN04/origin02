{{#if flashMsg}}
<div class="alert alert-{{flashMsg.type}}" role="alert">
   {{flashMsg.msg}} 
</div>
{{/if}}

<div class="row">
{{#if logined}}
<div>
   <h2>Hello {{loginUser}} Welcome back !!</h2><hr>
    <a href="/logout">Logout</a>
</div>
{{else}}
<div class="col-lg-6">
    <form method="post" action="#">
        <input type="hidden" name="hush" value="Hidden, But not secret !">
        <div class="form-group">
            <label for="user">user</label>
            <input type="text" class="form-control" name="user" id="user">
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            <input type="text" class="form-control" name="password" id="password">
            {{#if password}}
            <small>Last time keyin: {{password}}</small>
            {{/if}}
        </div>
        {{!-- Choice Color --}}
        <div>
            <labe for="color">Your favorite color:</labe>
            <input type="text" id="fieldColor" name="color">
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>
{{/if}}
</div>